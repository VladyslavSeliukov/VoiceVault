name: VoiceVault CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv and setup Python
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
          enable-cache: true
          cache-dependency-glob: "pyproject.toml"

      - name: Sync dependencies
        run: uv sync --all-groups

      - name: Run Ruff (lint)
        run: uv run ruff check .

      - name: Run Ruff (format check)
        run: uv run ruff format --check .

      - name: Run mypy
        run: uv run mypy .

  test:
    name: Run Pytest
    runs-on: ubuntu-latest
    needs: lint

    #    services:
    #      postgres:
    #        image: postgres:16-alpine
    #        env:
    #          POSTGRES_USER: ci_user
    #          POSTGRES_PASSWORD: ci_password
    #          POSTGRES_DB: voicevault_test_db
    #        ports:
    #          - 5432:5432
    #        options: >-
    #          --health-cmd pg_isready
    #          --health-interval 10s
    #          --health-timeout 5s
    #          --health-retries 5
    #
    #      redis:
    #        image: redis:8-alpine
    #        ports:
    #          - 6379:6379
    #        options: >-
    #          --health-cmd "redis-cli ping"
    #          --health-interval 10s
    #          --health-timeout 5s
    #          --health-retries 5
    #
    #       ── Phase 6+ ── uncomment once RabbitMQ-based tasks exist
    #       rabbitmq:
    #         image: rabbitmq:4-management-alpine
    #         ports:
    #           - 5672:5672
    #         options: >-
    #           --health-cmd "rabbitmq-diagnostics check_port_connectivity"
    #           --health-interval 10s
    #           --health-timeout 5s
    #           --health-retries 5
    #
    #       ── Phase 9+ ── uncomment once vector search tests exist
    #       qdrant:
    #         image: qdrant/qdrant:latest
    #         ports:
    #           - 6333:6333

    env:
      ENVIRONMENT: "test"
      LOG_LEVEL: "INFO"

      BOT_TOKEN: "test_token_for_ci"

      VOICES_DIR: "storage/voices"

      WHISPER_CPP_DIR: "/path/to/your/whisper.cpp"
      WHISPER_MODEL: "ggml-base.bin"

      WHISPER_PORT: 8080
      WHISPER_HOST: "host.docker.internal"

      HOST_OBSIDIAN_DIR: "/path/to/your/obsidian/folder"
      OBSIDIAN_DIR: "/app/storage/obsidian"

      LLM_API_BASE: "http://testhost:1234/v1"
      LLM_MODEL: "test-9b-llm"

      REDIS_URL: "redis://localhost:6379/0"
      FLUSH_TIMEOUT_MINUTES: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv and setup Python
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
          enable-cache: true
          cache-dependency-glob: "pyproject.toml"

      - name: Sync dependencies
        run: uv sync --all-groups

      - name: Run Pytest
        run: uv run pytest -v
