ENVIRONMENT=test
BOT_TOKEN=test_token

RABBITMQ_USER=guest
RABBITMQ_PASS=guest

WHISPER_PORT=8080
WHISPER_HOST=localhost
WHISPER_CPP_DIR=/tmp
WHISPER_MODEL=model.bin

LLM_MODEL=llama3
EMBEDDING_MODEL=nomic

HOST_OBSIDIAN_DIR=/tmp
